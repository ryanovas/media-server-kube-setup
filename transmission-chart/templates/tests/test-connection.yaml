apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "transmission-chart.fullname" . }}-test-connection"
  labels:
    {{- include "transmission-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "transmission-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
